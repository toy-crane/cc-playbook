---
description: List all available custom slash commands grouped by user and project.
---

List all available custom slash commands from the "Available Commands" section in your system prompt.
Group them by location:
- User commands (user) - from ~/.claude/commands/
- Project commands (project) - from .claude/commands/

Only list custom slash commands. Do not include skills, built-in CLI commands, or other features.
